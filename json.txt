import json

# some JSON:
x = '{"name":"John", "age":30, "city":"New York"}'
xx = '{"name":"John", "other": {"city":"New York"}}'

# parse x:
y = json.loads(x)
yy = json.loads(xx)

# the result is a Python dictionary:
print(y["age"])
print(y["other"]["city"])

json.dump(object) # or dumps
json.load()

also model_dump

